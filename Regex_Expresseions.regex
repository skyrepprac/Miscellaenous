Find the nth occurrence of any given tag in any type of file like .phtml or .html or .xml:
	Find Pattern: (?s)(?:<td(?:.|\s)*?){n-1}(<td)
	Example: (?s)(?:<td(?:.|\s)*?){4}(<td) - Finds the 5th occurrence of the <td> tag in file.
	Example: (?s)(?:<td(?:.|\s)*?){5}(<td) - Finds the 6th occurrence of the <td> tag in file.
	Example: (?s)(?:<div(?:.|\s)*?){5}(<div) - Finds the 6th occurrence of the <div> tag in file.

Replace 2 or more spaces around '=>' sign with single space around the same:
	Find Pattern: \s{2,}(=>)\s{2,}
	Replace with:  => 
